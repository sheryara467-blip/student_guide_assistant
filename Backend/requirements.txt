# =============================================================================
# requirements.txt — Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt

# ── API Framework ─────────────────────────────────────────────────────────────
fastapi      # Web framework for building the REST API
uvicorn[standard] # ASGI server to run FastAPI

# ── AI / LLM ─────────────────────────────────────────────────────────────────
groq              # Groq LLM API (answer generation)
# sentence-transformers==3.0.1 # Local embedding model for Pinecone queries
fastembed

# ── Vector Database ───────────────────────────────────────────────────────────
pinecone           # Pinecone vector database client

# ── PDF Processing ────────────────────────────────────────────────────────────
pdfplumber       # Extracts text from PDF files (better than PyPDF2 for layout)

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic          # Request/response schema validation

# ── Environment Variables ─────────────────────────────────────────────────────
python-dotenv     # Loads .env file into environment variables


httpx